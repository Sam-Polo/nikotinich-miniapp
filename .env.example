# URL бэкенда для сборки фронтенда (передаётся как build arg в Dockerfile).
# Локально: http://localhost:4001
# На сервере: публичный URL, например https://api.example.com
VITE_API_URL=http://localhost:4001
